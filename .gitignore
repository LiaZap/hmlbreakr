# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — NUNCA commitar (Vite injeta no build, sobrescrevendo Easypanel)
.env
.env.local
.env.production
.env.development
server/.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local dev database
server/prisma/dev.db
server/prisma/schema.dev.prisma

# Local testing
docker-compose.local.yml
test-local.bat
test-local-stop.bat
server/.env.local
server/.env.production.backup
server/.env.prod-backup
server/.env.*-backup
server/backup-*.json
server/backups/
TEST_BPO_LOCAL.md
BPO_E2E_TESTS.md

# Ad-hoc scripts/docs locais
bulk-restore.html
restore-pampa.html
migration-monday.md

# Seed samples gerados (PDFs binarios pesados — gerar via npm run seed:samples)
server/scripts/samples/*.pdf
# (xml, ofx, csv, xlsx, txt em scripts/samples/ SAO commitados)

# Claude Code config (local)
.claude/
